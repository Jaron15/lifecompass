import React from 'react';

const StreakMessage = ({ streakCount, maxStreak }) => {
    return (
        <div className="streak-message text-xl mt-2">
            Keep it up!
        </div>
    );
};

export default StreakMessage;
